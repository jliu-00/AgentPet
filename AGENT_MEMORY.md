# AgentPet 项目记忆文件 (Project Memory)

## 核心机制
- **状态追踪**：该宠物插件完全依赖底层大模型运行会话的 `transcript.jsonl` 文件判断工作状态。
- **判断逻辑**：程序读取 JSONL 文件的最后一行。若最后一行的类型为 `PLANNER_RESPONSE` 且没有 `tool_calls`，意味着大模型的回答彻底结束，处于空闲（IDLE）等待输入阶段。此时宠物执行休息动画。
- **休眠平滑**：判定结束时会保留 3 秒的冷却缓冲期（`workingUntil = Date() + 3.0`），防止闪烁抖动。
- **防止乱码中断**：通过截取最后 8192 字节可能会将 UTF-8 多字节字符拦腰斩断，此时严禁使用会返回 `nil` 的 `.utf8` 强校验编码，而必须使用 `String(decoding: data, as: UTF8.self)`，保证解析过程即使遇到非法前置字节也不会中断。

## 注意事项
- 由于代码补全（Autocomplete）操作不写入 `transcript.jsonl`，因此 IDE 的自动提示补全**不会唤醒**老鼠，只有进行正式对话或 Task 时才会唤醒。
- 不要尝试监听 `language_server.log`，这会导致代码补全触发宠物无意义的狂奔。
- 图标资源为纯色/线条构成的静态帧（猫和老鼠），切勿修改窗口透明层逻辑。

## 默认配置
- 默认宠物：老鼠 (Rat)
- 轮询间隔：0.5秒检查一次文件状态；0.1秒切一张动画帧。
